---
name: git-push
description: Push changes to a remote repository
---

# How to push changes to a remote repository

This is a two-step process:
1. Run `git push` to push changes to the default remote (origin)
2. Run `git push github <branch name>` to push changes to the GitHub backup
